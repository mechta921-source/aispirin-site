# GEO-страница + лид-магнит — ВЫПОЛНЕНО (25.06.2026)

## Сделано
- ✅ Страница `/geo-optimizaciya/` — в проде, проверено: title уникален, JSON-LD (Article/FAQPage/Question/Answer/Person), чек-лист из 5 шагов открыто в тексте, кейс (Экотермикс анонимно), FAQ, CTA с deep link `start=geo`.
- ✅ `sitemap.xml` и `llms.txt` — страница добавлена (проверено на живом сайте).
- ✅ Бот `diagnost_bot/bot.py` — ветка `start=geo` реализована (Задачи 5–7 плана): константа `S_GEO_DOMAIN`, функция `notify_andrey_geo()`, обработка `/start geo` (спрашивает только домен, без общей диагностики), уведомление Андрею. Баг плана с `lstrip("https://")` исправлен на `re.sub(r'^https?://', ...)`.
- ✅ Бот перезапущен (py_compile чистый, `@spirin_diagnost_bot` getMe ok), ровно один процесс.

## БЕЗОПАСНОСТЬ — требует действия Андрея
- ⚠️ Файл `geo-page-plan.md` был задеплоен в прод и публично отдавался (~16:15–18:xx 25.06). В нём — **токен diagnost_bot `8686615843:...`**. Файл удалён из `/var/www/aispirin/` (а также `_progress.md`, `README.md`, `setup.sh`). Токен по URL больше не отдаётся.
- 🔴 **Рекомендуется ОТОЗВАТЬ и пересоздать токен diagnost_bot в @BotFather** (`/revoke` → новый токен), т.к. он был публично доступен. После — обновить `TOKEN` в `diagnost_bot/bot.py` и перезапустить. Алекс обновит код, как только Андрей даст новый токен.

## На будущее (защита от повторной утечки)
Деплой копирует и `.md`/служебные файлы в веб-корень. Варианты: исключать их при деплое (rsync `--exclude='*.md' --exclude='.claude' --exclude='graphify-out' --exclude='setup.sh'`) или добавить в nginx `location ~ \.(md|sh)$ { deny all; }`. Не настроено — обсудить.
